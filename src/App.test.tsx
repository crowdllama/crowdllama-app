import { describe, it, expect } from "bun:test";

describe("App", () => {
  it("has basic structure", () => {
    expect(true).toBe(true);
  });

  it("can run tests", () => {
    expect(1 + 1).toBe(2);
  });
}); 